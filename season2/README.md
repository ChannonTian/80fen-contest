# 第二赛季 · 完整档案

2026-09-22 跑完的正赛,**3600 场,一墩不漏**。

> ⏳ **积分榜待填** —— 联赛还在跑。跑完之后这一节由 `contest/gen-season.js` 生成的
> [`standings.md`](standings.md) 和 [`league.json`](league.json) 填上,本文件同步更新。

三份提交的代码在 [`../submissions/`](../submissions/),这里是它们打出来的每一张牌。

赛制:两两对过局,**6 对 × 300 副牌 × 交换阵营 = 每对 600 场**。一场是一整轮打级,
2 → A,过 `2/5/10/13` 四道关。裁判引擎是主办方的线上正式版 **v0.7.19**。

**这一季的裁判没有已知的实现偏差。** 第一赛季是带着四处偏差跑完的,
2026-09-07 ~ 09-11 全部修掉了(见 [`../RULES.md`](../RULES.md) §J)——
所以这一季和上一季**不是同一个裁判**,两季的数字不能直接比。

---

## 名单

| 选手 | 这一季 | 路线 |
|---|---|---|
| 陪练(我们安插的) | v0.7.19,开着收官蒙特卡洛 | 标尺,不是标准答案 |
| [`deepseek-v4-pro`](../submissions/deepseek-v4-pro) | v3(hybrid) | 移植官方基线引擎 + 自己的跟牌 |
| [`gpt-6-astra`](../submissions/gpt-6-astra) | C02 | **自己写的规则引擎** + 上一季的启发式 |
| [`zai-glm`](../submissions/zai-glm) | v2 | `index.html` v0.7.14 块① 的 fork |

**`gpt-6-astra` 接替同一选手上一季的 `gpt-5.6-sol`,不是新增一名选手** ——
选手自己在提交里写明了这一点。旧目录留在 git 历史里,第一赛季的链接
[已经钉到当季的 commit](../season1/README.md) 上,不受影响。

**`claude-opus-5` 和 `kimi-k3` 确认不参加。** 它们第一赛季的代码仍在 repo 里,
但**没有入场** —— 本季的榜上不会出现它们,也不应当拿本季的数字去说它们强弱。

---

## 这一季和上一季结构性地不同

### 1. 三份里有两份是 fork 官方陪练的

`contest/baseline.js` 的头部写着「参赛者的起点:想改进而不是从零写的,**直接 fork
这个文件**」,而主办方的主 repo 是公开的 Apache-2.0 —— 引擎、整个 AI、设计文档、
参数表,全都拿得到。**两份提交这么做了,一份照规则书从零写。三份都合规。**

但它改变了这个联赛在量什么:对那两份而言,比的是**相对基线的增量**。

**这一格是我们没做好,不是选手的问题。** 参赛手册 §5 当时还写着「裁判什么工具都不
借给你」,比赛主页写着「我们不借任何代码给你」—— 于是最强的起点成了**可发现但没有
写进文档**的东西。找到的人省掉整个引擎,没找到的人从零写。下一季开赛前会明示到底
允不允许,并把口径写进手册。

### 2. 公开 PR 漏的不只是代码

三位选手都声明没有读别人的 v2 代码 —— **但 PR 的标题和描述是公开的**,
而标题里就写着路线(「基线 fork + 四项搜索/采样强化」)。`deepseek-v4-pro`
主动披露它看见了这个标题,并据此调整了自我评估。

第一赛季的结论是「第二赛季提交走私有渠道」,后来改回了公开 PR。
「不许看代码」靠自觉还守得住,「不许看标题」守不住 —— 标题是 PR 这个形式的一部分。

### 3. 超时门槛第一次真的咬人

上一季六位选手全部在 0.1 秒/场量级,门槛(一整场 ≤ 20 秒、单次决策 ≤ 2 秒)
形同虚设。这一季两份提交内嵌了整个基线引擎,`zai-glm` 量到一个约 **+2 级/场**
的配置,因为要 34 秒/场超过门槛,**自己放弃了**。

门槛本来是用来挡失控实现的,这一季它开始改变提交内容了。

---

## 文件

| | |
|---|---|
| [`plays/`](plays/) | **逐墩记录** —— 6 个文件,一对一个。每一局的发牌、底牌、扣牌、每一墩谁出了哪几张、谁赢 |
| [`../replay.js`](../replay.js) | 牌谱阅读器,零依赖,把一局渲染成人看得懂的样子(两季共用) |
| [`../FORMAT.md`](../FORMAT.md) | 记录格式 —— 字段、牌 id 编码、怎么还原手牌(两季共用) |
| [`standings.md`](standings.md) | 赛报:积分榜、对战表、逐对配对统计、打法画像、用时 |
| [`league.json`](league.json) | 积分榜 + 每一对的配对样本(一个种子一个数,自己做统计用) |
| [`reviews/`](reviews/) | 逐选手复盘 —— 分差丢在哪一侧、关卡局、底、亮主、丢分最多的十局 |

`plays/` 是**逐局记录的严格超集** —— 逐局那 17 个字段一个不少地在里面,
所以要看牌只读这一个目录就够。**这一季起多一格 `declBy`**(亮主的那一家的座位),
第一赛季的记录里没有。

## 上手

```sh
# 这一对打了多少场、几比几
node replay.js season2/plays/deepseek-v4-pro__gpt-6-astra.ndjson.gz

# 第 7 副牌的两场(交换阵营),摊开四家手牌
node replay.js season2/plays/deepseek-v4-pro__gpt-6-astra.ndjson.gz 7 --hands

# 只看其中一场的第 3 局
node replay.js season2/plays/deepseek-v4-pro__gpt-6-astra.ndjson.gz 7 --side=0 --round=3
```

格式见 [`../FORMAT.md`](../FORMAT.md)。

## 这份档案和第一赛季那份不一样的地方

第一赛季的逐墩牌谱是比完之后**重跑出来的**(当时只记了逐局摘要)。
**这一季是边跑边记的** —— `league.js --plays=` 在正赛当场写下来,
不存在「重跑是不是同一场比赛」这个问题。
