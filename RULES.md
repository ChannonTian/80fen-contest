# 80分(上海规则)—— 规则书

这一份是**权威规则**。比赛裁判的引擎与它逐条对应 —— 若两者不一致,那是裁判的
bug:公开修、受影响的对局重跑。你不需要去猜裁判的实现细节。

**三种读法,按你的目的挑:**

| 你是 | 读哪几部分 |
|---|---|
| 没打过 80 分,想学会打 | **第一部分**(入门) |
| 已经会打,只想查边界情形 | **第二部分**(裁决手册 A–J) |
| 要照这份规则**再写一个一样的游戏** | 第二部分 + **第三部分**(实现规格 S1–S6)。第三部分把每条规则写成可直接落地的判定,并给一套自测向量:你的实现跑通那张表,就和这一份是同一个游戏 |

术语用沪语和普通话两套(界面里可切换),第一次出现时都会标出来。

**目录**

- 第一部分 入门:[1 一句话](#1-一句话) · [2 牌、人、座位](#2-牌人座位) · [3 一局怎么走](#3-一局怎么走) · [4 谁大谁小](#4-谁大谁小--最要紧的一节) · [5 怎么算分怎么升级](#5-怎么算分怎么升级) · [6 六个坑](#6-新手最容易搞错的六件事)
- 第二部分 裁决手册:[A 牌序](#a-牌序的完整定义ordidx) · [B 牌型](#b-牌型decompose--classify) · [C 跟牌义务](#c-跟牌义务islegalfollow) · [D 甩牌](#d-甩牌沪语撒牌checkthrow) · [E 一墩胜负](#e-一墩的胜负resolvetrick) · [F 亮主链](#f-亮主链加固造反) · [G 结算抠底](#g-结算与抠底scoreround) · [H 连打与关卡](#h-多局连打轮庄必打关卡advancematch) · [I 可调项](#i-规则层面的可调项) · [J 简化与存疑](#j-当前的简化与存疑)
- 第三部分 实现规格:[S1 数据模型](#s1-数据模型与约定) · [S2 时序](#s2-一局的完整时序) · [S3 判定伪码](#s3-八个判定的伪码) · [S4 参数总表](#s4-参数总表) · [S5 自测向量](#s5-一致性自测向量) · [S6 十个坑](#s6-容易写错的十个地方)

---

# 第一部分:入门

## 1. 一句话

四个人两两搭档,用两副牌抢牌面上的 **200 分**。庄家方守住分数就升级,闲家方抢到 80 分就上台 ——
**谁先打过 A,谁赢整场。**

## 2. 牌、人、座位

* **两副牌共 108 张**(52×2 + 大小王各 2 张)
* **四人固定搭档**:南北一队、东西一队,对家是队友
* 每人 **25 张**,剩下 **8 张**扣进**底牌**(沪语「底」),归庄家
* 只有三种牌有分:**5 = 5 分,10 = 10 分,K = 10 分**,合计 **200 分**

**座位与转向**。四家按 **南 → 东 → 北 → 西 → 南** 的顺序轮转(即逆时针),
出牌、发牌、轮庄都用这一个方向:

```
            北(你的队友)
              ▲
    西 ──────┼──────► 东     轮转方向:南→东→北→西
              │
            南(你)
```

* **下家** = 你的下一位(南的下家是东),**上家** = 你的上一位(南的上家是西),**对家** = 队友
* 代码里座位记为 0=南、1=东、2=北、3=西;下家 = `(seat+1)%4`,对家 = `(seat+2)%4`,
  同队 = 座位号奇偶相同

一方是**庄家方**(做庄的和他对家),另一方是**闲家方**(沪语常说「抓分方」)。
闲家方要抢分,庄家方要拦。

## 3. 一局怎么走

**① 定谁先拿牌。**

* **整场第一局**(以及完全造反之后的那一局):每人从牌堆切一张,最大的先拿 ——
  比自然大小(A 最大、2 最小,王比 A 还大),同点数比花色 **♠>♥>♣>♦**。
* **其余各局**:**庄家先拿牌**(上一局的结果已经定好了谁坐庄,见第 5 节)。

**② 一边发牌一边亮主。** 牌是**一张张发**的,从先拿牌的人起按座位顺序轮转,
一人一张发满 100 张;**剩下的 8 张就是底牌**,先扣着不给任何人看。

发的过程中随时可以**亮主**(沪语「亮将」):亮出一张跟**当前级数**相同的牌,
那张牌的花色就是**主花色**。比如现在打 2,你亮一张 ♥2,红桃就是主。
**只能用已经拿到手的牌亮** —— 这就是为什么亮主是个「越早越冒险、越晚越可能被人抢先」的选择。

别人可以用更强的方式盖过去(见第 4、F 两节)。发完最后一张之后**还留一小段时间**
(默认 10 秒)给大家最后亮/反,这段时间过完,主就定死了。

**③ 弱牌可以掀桌(可选规则,默认关)。** 主定下来之后,谁的牌太差
(手牌分 ≤ 15,或主牌 ≤ 3 张)可以要求**重新发牌**;按设置还可能连庄家一起重抢。
详见 F 节。不用这条规则的话,这一步直接跳过。

**④ 庄家拿底、扣底。** 庄家把 8 张底牌拿进手里(变成 33 张),再从这 33 张里挑 8 张扣回去。
**扣什么都行** —— 分牌、主牌都可以扣。扣回去的牌**别人看不见**,但里面的分**还算数**:
这是这个游戏最要命的一处,见第 5 节。

**⑤ 打 25 墩。** 从**庄家**开始领出(沪语「出牌」),按座位顺序一人出一手,
四手比大小,大的收走这一墩的分,下一墩由他领出。

**⑥ 结算升级。** 数闲家方一共收了多少分,决定谁升几级、下一局谁坐庄。

## 4. 谁大谁小 —— 最要紧的一节

### 主牌和副牌

定了主花色之后,牌分成两类:

* **主牌**(沪语「将牌」):大王、小王、**所有花色的级数牌**、主花色的其余牌
* **副牌**:另外三门花色

注意第三条:**级数牌不管什么花色,全是主牌**。打 2 的时候,♠2 ♥2 ♣2 ♦2 全都是主牌。

### 主牌内部的大小(从大到小)

```
大王  >  小王  >  正级牌  >  副级牌  >  主A > 主K > 主Q > … > 主3
                                       (级数那一张已经抽走了)
```

* **正级牌** = 主花色的级数牌。打 2、主是红桃 → ♥2
* **副级牌** = 其他三门的级数牌。♠2 ♣2 ♦2 —— 三张一样大

副牌之间**不比大小**:一墩里领出的是红桃,你出一张黑桃,那张黑桃再大也不算数(除非黑桃是主)。

### 一墩谁收

1. 大家跟的都是同一门 → 最大的那张收
2. 有人**没有这门**、拿主牌打了下去(沪语「敲脱」,普通话「毙牌」)→ 主牌收
3. 几个人都毙了 → 主牌里最大的那个收
4. 出的牌**结构不一样**就不参与比较 —— 领出一对,你出两张单牌,那两张不算数
5. **一样大的,先出的收**(只在无主局的异花级数牌之间会遇到)

## 5. 怎么算分,怎么升级

数的是**闲家方**收到的分。

| 闲家方收到 | 结果 |
|---|---|
| 0 分 | 庄家方升 **3** 级(沪语「关光」) |
| 1–39 分 | 庄家方升 **2** 级 |
| 40–79 分 | 庄家方升 **1** 级 |
| 80–119 分 | 闲家方**上台**(换他们做庄),不升级 |
| 120–159 分 | 闲家方上台,升 **1** 级 |
| 160 分以上 | 上台,每多 40 分再升 1 级 |

### 抠底 —— 底牌里的分怎么算

那 8 张底牌里的分,**平时不算给任何人**。但如果**闲家方赢下最后一墩**,底牌的分就**翻倍算给闲家方**
(沪语「灭底」「翻」):

> 底分 × 2 × 最后一墩每人出的张数

最后一墩出单张 → **×2**;出一对 → **×4**;出两连对的拖拉机 → **×8**。

所以底里有 20 分、闲家赢了最后一墩打单张,那就是 **40 分**凭空进账 —— 一局才 200 分,
这一下能直接决定输赢。这也是为什么庄家扣底时要少扣分,以及为什么整局都要留一手守最后一墩。

### 下一局谁坐庄

* 庄家方**守住**(闲家 < 80 分)→ 庄家方升级,**前庄的对家**接着做庄(连庄)
* 闲家方**上台**(≥ 80 分)→ 闲家方升级,**前庄的下家**做庄

### 升级到哪算赢

两队各自记级数,都从 **2** 打起。**打过 A 就赢整场**(在 A 上做庄并守住,或从 A 再升一级)。
每局的常主 = **庄家方当前的级数**,所以两队级数不同时,主牌那一档也跟着变。

嫌一场太长,设置里可以打开**速通模式**:级数只走 2 / 5 / 10 / K / A,
每局最多前进一档,一整场最多 4 局(详见 I)。

## 6. 新手最容易搞错的六件事

1. **副级牌也是主牌。** 打 2 的时候 ♦2 是主牌,不是方块。别拿它当废牌垫掉。
2. **副牌之间不分大小。** 领出红桃你出黑桃,黑桃 A 也收不走 —— 除非黑桃是主。
3. **有这门必须跟。** 手里还有红桃就必须出红桃,不能垫别的。
4. **门里有对必须出对。** 领出一对红桃,你手里有红桃对子就必须拆出来跟。
5. **80 分只是上台,不是升级。** 抢到 80 分只换庄;要升级得 120 分起。
6. **底牌的分,只有赢下最后一墩才拿得到 —— 但一拿就翻倍。**

---

# 第二部分:裁决手册

以下每条都对应引擎里的一个函数,括号里是函数名,方便对照代码;
要照着实现的,配套伪码在第三部分 S3。

## A. 牌序的完整定义(`ordIdx`)

有主局,从大到小:

| 序号 | 是什么 |
|---|---|
| 15 | 大王 |
| 14 | 小王 |
| 13 | 正级牌(主花色的级数牌) |
| 12 | 副级牌(其余三门的级数牌,三张同级) |
| 11…0 | 主花色 A K Q J 10 … 2,**去掉级数那一张** |

序号 11…0 的算法:把 2…A 按自然顺序排开、抽掉级数那一张,剩下 12 个位置从 0 起编号 ——
所以主花色最大的散牌(A)恒为 11,最小的恒为 0,**不管级数是几**。
打 2 时 ♠3 = 0、♠A = 11;打 5 时 ♠2 = 0、♠A = 11。

**无主局**(王对造反成功)时:只有大小王和四门级数牌是主牌,**四门级数牌一律同级(13)**。
同级相碰,**先出的那家大**。副牌之间照旧不比大小。

序号只在**同一门内部**可比。跨门比较由 E 节的规则处理,不是比序号大小。

## B. 牌型(`decompose` / `classify`)

一手牌必须**全是同一门**(主牌算一门),否则不成型 —— 领出时会被判非法,跟牌时永远赢不了。

* **单张**
* **对子** —— 花色和点数都相同的两张。♥5+♥5 是对子;**♥5+♦5 不是**
* **拖拉机**(沪语「飞机」)—— **牌序上相邻**的两个或更多对子
* **甩牌**(沪语「撒牌」)—— 同一门里两个以上互不相连的组件,只有领出方能打,见 D

按 A 表的序号,相邻即可连。所以这些都能连:

```
主A对 + 副级牌对        (11 → 12)
副级牌对 + 正级牌对     (12 → 13)
正级牌对 + 小王对       (13 → 14)
小王对 + 大王对         (14 → 15)
```

**异花的两个副级牌对不能连** —— ♠2♠2 和 ♦2♦2 序号都是 12,**同级不算相邻**。
同理**主 A 对 + 正级牌对也不能连**(11 → 13,中间隔着副级牌那一档)。

副牌内部:去掉级数那一张之后按点数相邻。打 2 时 ♥3♥3+♥4♥4 是拖拉机;
打 7 时 ♥6♥6+♥8♥8 也是拖拉机(7 已经被抽走,6 和 8 在牌序上紧挨着)。
反过来,**打 2 时 ♥6♥6+♥8♥8 不是拖拉机**,是甩牌 —— 中间还夹着 7。

> 「这明明是拖拉机,为什么算甩牌」是这个游戏最常见的争议。判据只有一条:
> **在当前牌序里紧邻**。级数牌已从本门牌序里抽走,主门里正副级牌与大小王另算三档。

## C. 跟牌义务(`isLegalFollow`)

按顺序判定:

1. **张数**必须和领出的一样多
2. **有这门必须跟**:手里这门有几张就必须出几张(不够则全出,剩下的随便配)
3. **门内有对必对**:领出含 N 个对子,你这门有 M 个对子,必须至少出 **min(N, M)** 个对子
4. **拖拉机**:领出长度 L 的拖拉机
   * 你这门的最长拖拉机 **≥ L** → 必须出一个长度 ≥ L 的(更长的可以拆出子拖拉机)
   * 你只有更短的(长度 m,2 ≤ m < L)→ **也必须跟出你最长的那个**
     (这条是可调的:`partialTractorFollow`,默认开。各家规则不一,查到的公开规则只写「有拖拉机必须跟」,
     没写长度不匹配怎么办)

义务只在**本门**内成立。这门没有了,剩下的怎么配都行 —— 可以毙,也可以垫。
第 2、3、4 条是**独立叠加**的:先满足张数,再满足对子数,再满足拖拉机 ——
不是「满足一条就行」。

拖拉机那条的两个细节,实现时最容易漏:

* 「拆出子拖拉机」指的是**从更长的拖拉机里切出连续的一段**。手持三连对、
  领出两连对时,出 55-66 合法(切出的两连对),出 55+77 不合法(不相邻)。
* 领出的是拖拉机时才判这一条;领出的是甩牌里含拖拉机的,只按第 3 条数对子数。

## D. 甩牌(沪语「撒牌」,`checkThrow`)

**只有领出的人能甩。** 甩牌 = 一次打出同一门的多个组件(比如一对 + 一张单),赌没人管得住。

* 判定只跟**本门**比:引擎逐个组件去问其余三家,**手里这门**有没有牌能压过这个组件
* **任何一家能压任何一个组件**,甩牌就算失败
* 失败的后果:**只能出其中最小的那一组,不罚分**(「最小」= 组件里 `top` 序号最低的那一组,
  可能是一对而不是单张)

「能压过」逐类判定:

| 组件 | 别家这门有什么算压得住 |
|---|---|
| 单张 | 任意一张序号更大的牌 |
| 对子 | 序号更大的**对子或拖拉机** |
| 长度 L 的拖拉机 | 长度 ≥ L 且序号更大的拖拉机 |

裁决细节两条:

* 判定包括**队友** —— 只看牌,不看是谁的
* 判定只看**本门**,不看谁能毙。别人断门有主牌能毙,不影响甩牌成不成立

**判成甩牌但你觉得不该是甩牌**,先看出牌记录:界面会打印拆解结果(拆成几组、每组是什么)。
最常见的情形是主门里 6677 这类:打 6 或打 7 的时候,级数牌那一张在牌序上是另一档,
6 和 7 不相邻,所以是两个对子而不是拖拉机 —— 这是正解。

## E. 一墩的胜负(`resolveTrick`)

1. 后出的牌**结构必须和领出的一样**才参与比较(领出对子,单张两张不算;领出两对拖拉机,
   两个不相连的对子也不算;领出甩牌,组件构成一模一样才算)
2. 同门比 `top`(该手最大那张的序号),**大的收**
3. 主牌压副牌
4. 都是主牌 → 序号大的收;**序号相同则先出的收**
5. 这一墩的分归收牌那一方,**所有四家出的牌都算**

「结构一样」的准确含义:把两手牌各自拆成组件,**组件类型的多重集合相同**
(单张记 `single`,对子记 `pair`,长度 L 的拖拉机记 `tractorL`)。
所以领出「一对 + 一张单」时,只有同样是「一对 + 一张单」的才有资格比。

## F. 亮主链、加固、造反

**亮主链**(从弱到强,`declOptions` / `jokerPairOf`):

```
单张级数牌  <  一对级数牌  <  小王对  <  大王对(→ 无主局)
```

* **反主**要求**强度严格更高**;而且**不能反自己** —— 已经亮过的人想加强,只有「加固」这一条路。
  改花色、或者用王对把自己的主反成无将,都不是加固,是自己打乱自己(`canOverride` 会拒绝)
* **加固**(单张→亮成一对):**只有亮主的人自己**能加固,**同一花色**,而且**必须在任何造反之前**。
  队友不能替他加固
* **反主 / 王对造反**:在**已经定庄**的一局里,只改主色,**不换庄、不重拿底**
* **无庄局**(整场第一局,或完全造反之后的抢庄局):**最后亮主的人坐庄、拿底**;
  无人亮主 → 无主局,**先拿牌的人坐庄**
* 无庄局里,**各家按自己队伍的级数抢亮**,谁亮到,这一局就打谁的级数;
  无人亮主则按**先拿牌那一队**的级数。庄定局则全桌统一用庄家方的级数

**完全造反**(`canFullRebel`,可选规则,默认关):手牌太差可以掀桌重来,满足任一条即可 ——

* 手牌分数 ≤ **15**(`pointRebelThreshold`,5 的倍数、≤30,0 = 关闭)
* 手牌主牌张数 ≤ **3**(`trumpRebelThreshold`,-1 = 关闭)

判定在**主定下来之后**做(不然算不出「主牌几张」)。触发后重新发牌:

* 设置为「仅重新发牌」→ 换一副牌,庄家不变;
* 设置为「完全造反」→ 换一副牌,并且新的一局按**无庄局**处理:双方各按自己的级数抢亮,
  可被一对级数牌或王对反,胜者坐庄拿底、按自己的级数打。

同一局最多连续重发 `maxRedeal` 次(默认 3),超了就直接开打,免得卡死。

## G. 结算与抠底(`scoreRound`)

```
闲家方总分 = 闲家方各墩收到的分
           + (闲家方赢下最后一墩 ? 底分 × 抠底倍数 : 0)

抠底倍数 = 2 × 最后一墩每人出的张数
```

| 闲家方总分 | 判定 |
|---|---|
| 0 | 庄家守住,升 3 级 |
| 1–39 | 庄家守住,升 2 级 |
| 40–79 | 庄家守住,升 1 级 |
| ≥80 | 闲家上台,升 `floor((总分 − 80) / 40)` 级 |

所以 80–119 分是**上台但不升级**。
庄家方赢下最后一墩时,底牌的分**不算给任何人**(不是算给庄家)。

## H. 多局连打、轮庄、必打关卡(`advanceMatch`)

**轮庄**:

* 庄家方守住 → 庄家方升级,**前庄的对家**接着做庄(连庄)
* 闲家方上台 → 闲家方升级,**前庄的下家**做庄

**级数**:两队各记各的,常主 = 当局庄家方的级数。任一队**打过 A**(超过 14)整场结束。

**必打关卡**(可选,默认开;关卡级 **2 / 5 / 10 / K**)。两条判据,缺一不可:

1. **不可跳级** —— 升级途中跨过关卡就停在关卡上。打 3 该升 3 级本应到 6,5 是关卡 → 停在 5。
   落点**正好等于**关卡不算跨过(打 3 升 2 级到 5,正常到达);已经**站在**关卡上再升级,
   也不会被同一个关卡再拦一次
2. **必须打过** —— 过这一关的条件是本队在这一级**坐庄并守住**过

第 2 条容易被忽略:一局打的是**庄家方**的级数。所以停在 2 上当闲家、上台升一级,
其实**从没打过这一关**;这时只换庄不升级,下一局由这一方坐庄打这一关。
**坐庄丢庄也不算过关** —— 被人从这一级上打下去,不等于打过了。

不会卡死:停在关卡上的一方下一局必坐庄(守住是对家连庄、上台是自己坐庄),
只是可能要在同一级上反复坐几局,直到真正守住。关卡开关关闭时两条都不生效。

## I. 规则层面的可调项

界面「设置」里能改的规则参数(`RULES` 对象):

| 参数 | 默认 | 含义 |
|---|---|---|
| `levelStart` | 2 | 从几打起 |
| `handSize` / `kittySize` | 25 / 8 | 每人手牌 / 底牌张数 |
| `strictTractorFollow` | 开 | 领出拖拉机、手中有同长(或更长可拆)拖拉机时必须跟 |
| `partialTractorFollow` | 开 | 领出长拖拉机、手中只有短拖拉机时是否必须跟(上一条关掉时本条自动失效) |
| `pointRebelThreshold` | 15 | 手牌分 ≤ 此值可完全造反(0 = 关闭) |
| `trumpRebelThreshold` | 3 | 手牌主牌 ≤ 此值可完全造反(−1 = 关闭) |
| `maxRedeal` | 3 | 同一局最多连续重新发牌次数 |
| `offsuitRankTractor` | 开(不可关) | 主 A 对与副级牌对可连,见 B |
| 必打关卡 | 开,2/5/10/K | 见 H |
| 完全造反 | 默认关 | `off` / 仅重新发牌 / 完全造反 |
| `speedRun` / `speedLadder` | 关,2/5/10/K/A | **速通模式**,见下 |

### 速通模式

一整场从 2 打到 A,正常要打十几局。速通模式把级数压到**只走 2 → 5 → 10 → K → A**
五档,**每赢一局最多前进一档**(赢 3 级也只前进一档,赢 1 级同样前进一档)。
一整场因此最多 4 局。

打开时**必打关卡自动失效** —— 这条阶梯本身就是关卡,再叠一层只会更慢。
它只改升级的换算,发牌、跟牌、结算、抠底全部照旧。

## J. 当前的简化与存疑

诚实记录,以后可能会改:

* **完全造反后的重抢是一轮制明牌比强**,没有逐张重新发牌
* **无人亮主时先拿牌者坐庄** —— 待实战确认是否合上海习惯
* **`partialTractorFollow`** 没找到权威出处,默认开
* **完全造反的两个阈值**(15 分 / 3 张主)没找到权威出处,是拍的
* **扣底不设限制** —— 分牌、主牌都可以扣。有些房规禁止扣分或禁止扣主,这里不禁
* **不做「亮主的牌要留在手里到打完」这类约束** —— 亮出的牌只是标记,照常可以打出去
* 沪语术语表在代码里的 `TERMS`(大怪/小怪、将牌、无将、飞机、撒牌、敲脱、氽废、翻、造反、灭底、老K、皮蛋、J钩、打上去)

---

# 第三部分:实现规格(照着写一个一样的游戏)

这一部分写给**要重新实现这套规则**的人。上面两部分是给人读的,这一部分是给人**照着写**的:
每条规则都收敛到可判定的形式,并给一套自测向量。

**范围**:这里只管规则引擎(什么合法、谁赢、算几分)。AI 怎么选牌不在这一份 ——
那正是比赛要你自己想的。

## S1. 数据模型与约定

**牌**。一张牌是 `{suit, rank, id}`:

| 字段 | 取值 |
|---|---|
| `suit` | `'S'` ♠ / `'H'` ♥ / `'D'` ♦ / `'C'` ♣ / `'X'`(王) |
| `rank` | 2…14(11=J,12=Q,13=K,14=A);王固定 15=小王、16=大王 |
| `id` | 0…107,**全场唯一**。两副牌里同花色同点数的两张,`suit`/`rank` 相同而 `id` 不同 |

`id` 不是装饰:一手牌的合法性要校验「这些牌真的在你手上、且没有重复引用同一张」,
没有唯一 id 就分不清「我有两张 ♥5」和「我把同一张 ♥5 用了两次」。

牌堆的构造顺序不重要,但**必须恰好是**:四门 × 2…14 × 2 副 + 大小王各 2 张 = 108 张,
总分 200。

**分值**:`rank===5 → 5`;`rank===10 或 13 → 10`;其余 0。王不带分。

**主**。`trump = {suit, rank}`。`suit` 为 `null` 表示**无主局**;
`rank` 是当局级数(常主),恒等于当局庄家方的级数(无庄局里等于最终亮主者那一队的级数)。

**座位**。0…3,`(seat+1)%4` 是下家,同队 = 奇偶相同。庄家方 = `declSeat%2`。

**一手牌(component)**。`decompose` 的输出元素:
`{type:'single'|'pair'|'tractor', len?, cards, top}`,`top` 是该组件里最大那张的 `ordIdx`。

**一手牌的分类(`classify` 的输出)**:`{type, suit, top, cards, len?, comps?}`,
`suit` 是**有效花色**(主牌一律 `'T'`),`type` 多一个 `'throw'`(甩牌)。

## S2. 一局的完整时序

```
① 定先拿牌者
   首局/完全造反后 → 切牌比大小(A>…>2,王最大;同点 ♠>♥>♣>♦)
   其余局           → 庄家先拿

② 洗牌、逐张发 100 张(先拿者起,按 (first+i)%4 轮转),末 8 张 = 底牌
   发牌途中随时可亮主 —— 只能用**已经拿到**的牌
   发完后再留一段亮主窗口(默认 10 秒),窗口结束前仍可亮/反/加固

③ 定主:最后一个成立的亮主决定主花色(无人亮 → 无主局)
   庄定局:庄家不变;无庄局:最终亮主者坐庄,无人亮主则先拿牌者坐庄

④ (可选)低分/少主造反:任一家满足阈值可要求重发
   → 重发则回到 ①(完全造反模式下,新的一局按无庄局处理)
   → 连续重发达到 maxRedeal 次则强制开打

⑤ 庄家把 8 张底牌收进手里(33 张),扣回 8 张(无限制)

⑥ 打 25 墩:庄家领出;每墩四家各出一手;resolveTrick 定胜负;赢家领出下一墩

⑦ 结算:scoreRound → advanceMatch(升级、轮庄、关卡)→ 下一局或整场结束
```

第 ③ 步与第 ④ 步的顺序不能颠倒:造反资格要数「手里几张主牌」,主没定就数不出来。

## S3. 八个判定的伪码

以下伪码与引擎逐行对应,变量名即函数名。

**① 有效花色 `effSuit(c, trump)`** —— 「这张牌算哪一门」

```
if c.suit === 'X'            return 'T'      // 王
if c.rank === trump.rank     return 'T'      // 级数牌,不论花色
if trump.suit && c.suit === trump.suit return 'T'
return c.suit
```

**② 牌序 `ordIdx(c, trump)`** —— 只在同一门内可比

```
if c.rank === 16 return 15                   // 大王
if c.rank === 15 return 14                   // 小王
if c.rank === trump.rank:
    if trump.suit === null   return 13       // 无主:四门级数牌同级
    return c.suit === trump.suit ? 13 : 12   // 正级牌 / 副级牌
return indexOf(c.rank, [2..14] 去掉 trump.rank)   // 0…11
```

**③ 拆解 `decompose(cards, trump)`** —— 一手牌拆成组件

```
按 (suit,rank) 分组                          // 注意:同花同点才算一对
每组 ≥2 张 → 取两张成一个 pair;张数为奇数 → 余下一张成 single
pairs 按 ordIdx 升序排
扫描 pairs:相邻两对若 ordIdx 差恰为 1 → 接进当前 run,否则结束当前 run
run 长度 ≥2 → tractor(len = 对数,top = 最大那对的 ordIdx)
run 长度 =1 → pair
剩下的 singles 各自成 single
```

差**恰为 1**:同级(差 0)不相邻 —— 异花副级牌对、无主局的异花级数牌对都因此连不上。

**④ 分类 `classify(cards, trump)`**

```
空 → null
所有牌的 effSuit 必须相同,否则 null      // 混门 = 不成型
1 张 → single
decompose 后只有 1 个组件 → 那个组件的类型(pair / tractor)
否则 → throw(甩牌),top = 各组件 top 的最大值
```

**⑤ 跟牌合法 `isLegalFollow(hand, lead, chosen, trump)`**

```
chosen.length === lead.cards.length                    否则 false
chosen 全部来自 hand,且无重复 id                        否则 false

suitInHand  = hand 中 effSuit === lead.suit 的牌
chosenInSuit= chosen 中 effSuit === lead.suit 的牌
chosenInSuit.length === min(lead 张数, suitInHand.length)   否则 false   // 有则必跟、尽量跟

need = lead 里的对子数(pair=1,tractor=len,throw=各组件之和)
if need > 0:
    must = min(need, countPairsIn(suitInHand))
    countPairsIn(chosenInSuit) >= must                 否则 false

if strictTractorFollow 且 lead.type === 'tractor':
    m  = suitInHand 里最长拖拉机长度
    cm = chosenInSuit 里最长拖拉机长度
    if m >= lead.len 且 cm < lead.len                  false     // 有长必跟
    if partialTractorFollow 且 2 <= m < lead.len 且 cm < m   false   // 短拖也必须跟
true
```

`countPairsIn` 数的是 `⌊同花同点张数 / 2⌋` 之和,不看是否相邻。

**⑥ 一墩胜负 `resolveTrick(plays, trump)`**

```
lead = classify(plays[0])
best = lead;  winIdx = 0
for i in 1..3:
    cl = classify(plays[i])
    if cl 为 null 或 结构与 lead 不同 → 跳过         // 结构 = 组件类型多重集
    if cl.suit === best.suit:  if cl.top > best.top → 接管   // 严格大于:平手先出者胜
    else if cl.suit === 'T':                        → 接管   // 主牌压副牌
winner = plays[winIdx].seat
points = 四家出的所有牌的分之和
```

注意第二个分支不需要判「best 是不是主」:若 best 已是主,第一分支就命中了。

**⑦ 甩牌校验 `checkThrow(hands, seat, cards, trump)`**

```
lead = classify(cards);  若 type !== 'throw' → 通过
for 每个组件 comp:
    for 其余三家 p(含队友):
        sc = hands[p] 中 effSuit === lead.suit 的牌
        if canBeatComp(sc, comp): 
            forced = lead.comps 中 top 最小的那一组
            return {ok:false, forced}
return {ok:true}

canBeatComp(sc, comp):
    single  : sc 中存在 ordIdx > comp.top
    pair    : decompose(sc) 中存在 pair/tractor 且 top > comp.top
    tractor : decompose(sc) 中存在 tractor 且 len >= comp.len 且 top > comp.top
```

**⑧ 结算与推进**

```
scoreRound(defPoints, kitty, defWonLastTrick, lastLeadSize):
    mult  = 2 * lastLeadSize
    total = defPoints + (defWonLastTrick ? countPoints(kitty) * mult : 0)
    total < 80  → 庄家守住,升级数 = (total===0 ? 3 : total<40 ? 2 : 1)
    total >= 80 → 闲家上台,升级数 = floor((total - 80) / 40)

advanceMatch(levels, declSeat, sc, gates, played):
    declTeam = declSeat % 2
    若庄家守住:played[declTeam] = max(played[declTeam], levels[declTeam])   // 先记账再升级
    升级方 team / 名义升级数 up / 下一局庄家 dealer:
        守住 → team = declTeam,  dealer = (declSeat+2)%4
        上台 → team = 1-declTeam, dealer = (declSeat+1)%4
    速通模式:up>0 → levels[team] = speedLadder 里第一个大于当前级的档(没有则 +1)
    否则:
        level = clampAtGate(levels[team], levels[team]+up, gates)
        若 up>0 且 levels[team] 本身是关卡 且 played[team] < levels[team]:
            level = levels[team]        // 卡在这一关:只换庄不升级
    整场结束 = 任一队级数 > 14

clampAtGate(from, to, gates):
    to <= from → to
    hit = gates 中满足 g > from 且 g < to 的最小者    // 严格开区间
    hit 存在 → hit,否则 to
```

`played` 由调用方保存(引擎自己不留状态),记的是**每队坐庄守住过的最高级数**,初值 `[-1,-1]`。
不传 `played` = 只做第一层(不可跳级)。

## S4. 参数总表

| 常量 | 值 | 出处 |
|---|---|---|
| 牌张数 | 108(52×2 + 4 张王) | `makeDeck` |
| 每人手牌 / 底牌 | 25 / 8 | `RULES.handSize` / `kittySize` |
| 全场总分 | 200(5→5,10→10,K→10) | `cardPoints` |
| 起始级数 | 2 | `RULES.levelStart` |
| 抠底倍数 | 2 × 最后一墩每人张数 | `RULES.kittyMultiplier` |
| 上台线 | 80 分 | `scoreRound` |
| 每 40 分一级 | `floor((total−80)/40)` | `scoreRound` |
| 关光 / 升 2 / 升 1 | 0 / 1–39 / 40–79 | `scoreRound` |
| 切牌花色序 | ♠3 > ♥2 > ♣1 > ♦0,王最大 | `cutForFirst` |
| 亮主等待 | 10 秒(产品侧默认,非规则) | `S.declWait` |
| 造反阈值 | 15 分 / 3 张主 / 最多重发 3 次 | `RULES.*RebelThreshold` / `maxRedeal` |
| 关卡 | 2 / 5 / 10 / K,默认开 | `S.gates` |
| 速通阶梯 | 2 / 5 / 10 / K / A,默认关 | `RULES.speedLadder` |

## S5. 一致性自测向量

下表是引擎自测里的规则断言(浏览器打开 `#test` 可跑全套)。
**照这份规则实现的引擎,应当逐条给出同样的结果** —— 这是「是不是同一个游戏」的判据。
记号:`T = 主♠打2`(即 `{suit:'S',rank:2}`),`NT = 无主打2`;`P(H,5)` 表示一对 ♥5。

**牌序与主牌归属**

| 输入 | 期望 |
|---|---|
| `effSuit(♥2, T)` | `'T'`(副级牌是主) |
| `effSuit(♠9, T)` | `'T'`(主花色) |
| `effSuit(大王, T)` | `'T'` |
| `effSuit(♥9, T)` | `'H'` |
| `ordIdx` 排序 | 大王 > 小王 > ♠2 > ♥2 > ♠A |
| `effSuit(♥2, NT)` | `'T'`;`effSuit(♠A, NT)` = `'S'` |
| 无主局 | `ordIdx(小王,NT) > ordIdx(♥2,NT)` |

**牌型**

| 输入 | 期望 |
|---|---|
| `[♥5]` | single |
| `[♥5,♥5]` | pair |
| `[♥5,♦5]` | `null`(混门,不成型) |
| `P(H,5)+P(H,6)`,打 2 | tractor |
| `P(H,6)+P(H,8)`,**打 7** | tractor(7 已抽走) |
| `P(H,6)+P(H,8)`,**打 2** | throw(中间夹着 7) |
| `P(S,A)+P(H,2)`,T | tractor(11→12) |
| `P(H,2)+P(S,2)`,T | tractor(12→13) |
| `P(S,2)+P(小王)`,T | tractor(13→14) |
| `P(小王)+P(大王)`,T | tractor(14→15) |
| `P(H,2)+P(D,2)`,T | throw(异花副级牌同级,不相邻) |
| `P(S,A)+P(S,2)`,T | throw(11→13,隔着副级牌那一档) |
| `P(H,2)+P(小王)`,NT | tractor |
| `P(H,2)+P(D,2)`,NT | throw |
| `P(H,A)+[♥K]`,T | throw |

**跟牌义务**(领出 `P(H,10)`,除非另注)

| 手牌 | 出 | 期望 |
|---|---|---|
| ♥9♥9 ♥3 ♦4 ♠5 | ♥3+♥9 | **非法**(门内有对必对) |
| ♥9♥9 ♥3 ♦4 ♠5 | ♥9♥9 | 合法 |
| ♥3 ♥7 ♦4 | ♥3+♥7 | 合法(无对可拆两单) |
| ♥3 ♦4 ♦9 | ♥3+♦4 | 合法(本门只剩一张,全出) |
| ♥3 ♦4 ♦9 | ♦4+♦9 | **非法**(藏本门) |
| ♦4 ♦9 ♣3 | ♦4+♣3 | 合法(断门任出) |
| 领出 `P(H,10)+P(H,J)`;手持 ♥5♥5 ♥6♥6 ♥K♥K ♥3 ♦2 | ♥5♥5+♥K♥K | **非法**(有拖拉机必须跟) |
| 同上 | ♥5♥5+♥6♥6 | 合法 |
| 手持三连对 55-66-77 | 拆 55-66 跟 | 合法(子拖拉机) |
| 手持三连对 55-66-77 | 拆 55+77 跟 | **非法**(不相邻) |
| 领出三连对、手中只有二连对 | 拆散跟 | `partialTractorFollow` 关 → 合法;开 → 非法 |

**一墩胜负**(T)

| 一墩 | 期望 |
|---|---|
| ♥5 / ♥K / ♥A / ♥7 | 第三家(♥A)赢 |
| ♥A / ♠3 / ♥6 / ♥7 | 第二家(♠3 毙)赢 |
| ♥10♥10 / ♠3♠4 / ♥4♥4 / ♥6♥7 | 领出方赢(两张散主毙不了对子) |
| 无主局:♥5 / ♠A / ♥6 / ♦A | 第三家(♥6)赢(外门 A 只是垫牌) |

**甩牌**

| 局面 | 期望 |
|---|---|
| 甩 ♥Q♥Q+♥J,别家有 ♥K | 失败,被迫只出 ♥J,不罚分 |
| 甩 ♥A♥A+♥K,别家最大只有 ♥Q | 成功 |

**亮主 / 加固 / 造反**

| 输入 | 期望 |
|---|---|
| `declarationOf([♥2], 2)` | 亮 ♥,强度 1 |
| `declarationOf(P(小王), 2)` | 强度 3(无主) |
| `declarationOf([小王,大王], 2)` | `null`(大小王混对无效) |
| 用王对反一对级数牌 | 允许 |
| 自己已亮 ♠单张,再改亮 ♥对 / 小王对 / 大王对 | **一律拒绝**(不能反自己) |
| 别家用大王对反同一个 ♠单张 | 允许 |
| 亮主者本人、♥单张、未发生造反、手上有 ♥2♥2 | 可加固 |
| 已发生造反 / 非亮主者 / 已经是对 | 不可加固 |
| 15 分且无主牌的手牌 | 可完全造反 |
| 主 3 张、50 分的手牌 | 可完全造反(按主牌那条) |
| 主 4 张、20 分的手牌 | 不可造反 |

**定庄**

| 输入 | 期望 |
|---|---|
| 庄定局,庄家 1,亮主者 3 | 庄家仍是 1 |
| 庄定局,王对造反者 2 | 庄家仍是 1(只改主色) |
| 庄定局,无人亮主 | 庄家不变 |
| 无庄局,最终亮主者 3 | 3 坐庄 |
| 无庄局,无人亮主,先拿牌者 2 | 2 坐庄 |

**结算**(底牌 = ♥10 ♦K + 六张废牌,共 20 分)

| 输入 | 期望 |
|---|---|
| 闲家 75 分,未赢最后一墩 | 庄家方升 1 级 |
| 闲家 75 分,赢最后一墩,末墩单张 | 总分 75+20×2 = **115**,上台 |
| 闲家 35 分,赢最后一墩,末墩对子 | 倍数 4,总分 35+80 = **115**,上台 |
| 闲家 0 分 | 庄家方升 3 级(关光) |

**整场推进**

| 输入 | 期望 |
|---|---|
| `[2,2]`,庄家 1 守住升 2 级 | 队 1 到 4,下一局庄家 3(对家连庄) |
| `[2,2]`,闲家上台升 1 级 | 队 0 到 3,下一局庄家 2(前庄下家) |
| 闲家 80–119 | 上台,级数不动 |
| `[13,2]`,庄家守住升 3 级 | 整场结束,队 0 胜 |
| `[14,2]`,庄家守住升 1 级 | 整场结束(A 上守住即打过 A) |

**必打关卡**(关卡 = 2/5/10/K)

| 输入 | 期望 |
|---|---|
| `clampAtGate(3, 6)` | 停在 5 |
| `clampAtGate(3, 5)` | 到 5(落点正好是关卡,不算跨过) |
| `clampAtGate(5, 8)` | 到 8(从关卡出发正常越过下一段) |
| `clampAtGate(4, 12)` | 停在 5(一次跨两关,停最近的) |
| 关卡关闭 | 不拦 |
| 队 1 坐庄打 2,队 0 抢 150 分,`played=[-1,-1]` | 队 0 **停在 2**,只换庄不升级,下一局队 0 坐庄 |
| 同上但 `played=[2,-1]` | 队 0 升到 3 |
| 非关卡级数(3)遇同样局面 | 照常升到 4 |
| 庄家方在关卡级守住 | 不卡自己,并把这一级记进 `played` |
| 庄家方在关卡 10 **丢庄** | **不**记进 `played`;双方都仍卡在 10 |

**速通模式**

| 输入 | 期望 |
|---|---|
| 打 2 赢 4 级 | 到 5(不开速通是 6) |
| 5 赢 1 级 / 10 赢 3 级 / K 赢 2 级 | 10 / K / A |
| A 上再赢 | 整场结束 |
| 没升级 | 原地不动 |
| 同时开着必打关卡 | 阶梯即关卡,不再二次拦截 |

## S6. 容易写错的十个地方

1. **对子必须同花同点。** ♥5+♦5 不是对子。用 `(suit,rank)` 作键,不要只用 `rank`。
2. **同级不相邻。** 拖拉机要求 `ordIdx` 差**恰为 1**;异花副级牌对差 0,连不上。
3. **级数牌要从本门牌序里抽走。** 打 7 时 ♥6 和 ♥8 相邻,打 2 时不相邻。
   直接用点数差判相邻,这两种局面必错一种。
4. **跟牌义务是叠加的,不是择一的。** 张数、对子数、拖拉机三条要同时满足。
5. **「有则必跟」是 `min(领出张数, 本门张数)`,不是「有就全出」。**
   本门 5 张、领出 2 张时,出 2 张即可。
6. **结构不同的牌不参与比较,但仍然算分。** 垫进去的分牌照样归收牌方。
7. **平手先出者胜。** 比较要用严格大于;只有无主局的异花级数牌之间会真的撞上。
8. **抠底只在闲家赢最后一墩时生效,而且倍数跟末墩张数走**,不是固定 ×2。
9. **`played` 要在升级之前记账**,记的是「坐庄守住那一局的级数」;
   坐庄输了不记 —— 这是必打关卡最容易写反的一处。
10. **甩牌失败罚出的是 `top` 最小的那一组**,可能是一对,不是「张数最少的那一组」。

---

*本文与引擎同步。改了规则代码,记得回来改这里 —— 尤其是 D、G、H 三节,它们是最容易漂的;
改了判定逻辑,S5 那张向量表也要跟着核一遍。*
